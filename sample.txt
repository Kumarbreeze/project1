if you want to create a new branch in local, in order to follow the below steps

git branch <branch name>

git switch <branch name>


git pull origin main

git push origin <new branch name>


code commit

make any changes in respective file

git status

git add .

git commit -m "message"

git push -u origin feature4

if you want to change the branch: git switch <branch name>

-------------------------------------------------

pull new branch from git

- create a new branch in git with source as main
- use git pull
- git switch -c origin/<new branch>

-------------------------------------------------